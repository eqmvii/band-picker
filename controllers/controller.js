var db = require("../models");
var passport = require('passport');

// var Sequelize = require('sequelize');
const Op = db.Sequelize.Op; // query operators

// TODO: Right helpful helper query methods
function getMyBands(user) {
    return 12;
}
